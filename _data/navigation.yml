header:
  - titles:
      # @start locale config
      en      : &EN       Inicio
      # @end locale config
    url: /index.html

  - titles:
      # @start locale config
      en      : &EN       Quienes somos
      # @end locale config
    url: /nosotros.html

  - titles:
      # @start locale config
      en      : &EN       Evolucion
      # @end locale config
    url: /evolucion.html
